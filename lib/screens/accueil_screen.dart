import 'package:flutter/material.dart';

/// Page blanche — Accueil

class AccueilScreen extends StatelessWidget {
  const AccueilScreen();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
    );
  }
}
